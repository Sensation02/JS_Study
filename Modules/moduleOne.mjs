// const myName = 'Vasyl';
const sum = (a, b) => a + b;
const mult = (a, b) => a * b;

// export default sum; // такий експорт може бути тільки один раз

const one = 1;
const two = "two";

// експорт декількох перемінних:
export { one, two, sum, mult };
